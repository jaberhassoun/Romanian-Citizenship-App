function translateToEnglish(text: string) {
    // This function should call an API or use some library to translate text.
    // Here is a pseudo implementation. Replace it with actual API call.
    
    //future: grab the translation from an extermal JSON that has the quesiton pre-translated

    return "Translated text here";
  }